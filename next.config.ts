import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  basePath: '/crm',
  /* This tells the application it is securely nested inside the /crm folder path */
};

export default nextConfig;
